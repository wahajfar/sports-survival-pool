{
  "name": "sports-survival-pool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosting tournament survival pool — live standings site, bracket, and Slack alerts. Configure via config.json + data/roster.json.",
  "scripts": {
    "update": "node scripts/update.mjs",
    "live": "node scripts/live.mjs",
    "morning": "node scripts/morning.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0"
  },
  "engines": {
    "node": ">=20"
  }
}
